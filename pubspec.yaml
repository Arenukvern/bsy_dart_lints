name: bsy_dart_lints
description: Dart analyzer plugin with class layout lint rules and quick fixes.
version: 1.1.0
repository: https://github.com/example/bsy_dart_lints

environment:
  sdk: ^3.11.0

dependencies:
  analysis_server_plugin: ^0.3.10
  analyzer: ^10.2.0
  analyzer_plugin: ^0.14.4

dev_dependencies:
  analyzer_testing: ^0.2.1
  lints: ^6.0.0
  test: ^1.25.6
  test_reflective_loader: ^0.4.0
